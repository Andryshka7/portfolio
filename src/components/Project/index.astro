---
import { useTranslations } from 'i18n'
import type { Locale, Project } from 'types'

import { ImageCarousel, Icon } from 'components/ui'

interface Props extends Project {}

const { icon, title, images, source, figma, preview } = Astro.props

const locale = Astro.currentLocale as Locale
const t = useTranslations(locale)
---

<div class='mt-10 w-5/6 min-w-[300px] lg:w-3/4 2xl:w-[1120px]'>
    <div class='flex items-center justify-between'>
        <div class='flex items-center gap-2'>
            <img src={icon} class='h-16 sm:h-20' />
            <h3 class='mt-1 text-4xl font-bold text-zinc-800 dark:text-white'>{title}</h3>
        </div>

        <div class='mt-2 flex gap-1.5 md:gap-4'>
            <a href={source} target='blank'>
                <Icon
                    name='Code'
                    width={30}
                    height={30}
                    class='cursor-pointer duration-200 hover:scale-105 md:hidden'
                    color='white'
                />
                <button
                    class='hidden w-32 rounded-md border-2 border-red-400 py-0.5 text-center font-medium text-red-400 transition duration-300 hover:border-slate-400 hover:bg-slate-400 hover:text-zinc-200 dark:border-[#05CEC2] dark:text-teal-400 dark:hover:border-slate-200 dark:hover:bg-slate-200 dark:hover:text-midnight md:block'
                >
                    {t('ui.buttons.source')}
                </button>
            </a>
            <a href={preview} target='blank'>
                <Icon
                    name='Monitor'
                    width={30}
                    height={30}
                    class='cursor-pointer duration-200 hover:scale-105 md:hidden'
                    color='white'
                />
                <button
                    class='hidden w-32 rounded-md border-2 border-red-400 bg-red-400 py-0.5 text-center font-medium text-zinc-200 transition duration-300 hover:border-slate-400 hover:bg-slate-400 dark:border-[#05CEC2] dark:bg-[#05CEC2] dark:text-midnight dark:hover:border-slate-200 dark:hover:bg-slate-200 md:block'
                >
                    {t('ui.buttons.preview')}
                </button>
            </a>
        </div>
    </div>

    <ImageCarousel images={images} />

    <h2 class='mt-4 text-3xl font-bold text-zinc-800 dark:text-white'>
        {t('ui.labels.description')}
    </h2>
    <p class='mt-2 text-2xl font-normal text-zinc-800 dark:text-white'>
        {t(`projects.${title.toLowerCase()}.description`)}
    </p>

    <h2 class='mt-6 text-3xl font-bold text-zinc-800 dark:text-white'>
        {t('ui.labels.tech-stack')}
    </h2>
    <p class='mt-2 text-2xl font-normal text-zinc-800 dark:text-white'>
        {t(`projects.${title.toLowerCase()}.tech-stack`)}
    </p>

    <h2 class='mt-6 text-3xl font-bold text-zinc-800 dark:text-white'>
        {t('ui.labels.development-process')}
    </h2>
    <p class='mt-2 text-2xl font-normal text-zinc-800 dark:text-white'>
        {t(`projects.${title.toLowerCase()}.development-process`)}
    </p>

    <h2 class='mt-6 text-3xl font-bold text-zinc-800 dark:text-white'>
        {t('ui.labels.links')}
    </h2>

    <p class='mt-2 text-2xl font-normal text-zinc-800 underline-offset-4 dark:text-white'>
        <a href={figma} target='blank' class='underline'>{t('ui.links.figma-file')}</a>
        <span class='mx-1'>|</span>
        <a href={source} target='blank' class='underline'>{t('ui.links.source-code')}</a>
        <span class='mx-1'>|</span>
        <a href={preview} target='blank' class='underline'>{t('ui.links.live-version')}</a>
    </p>
</div>
